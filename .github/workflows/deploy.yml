name: Deploy

on:
   schedule:
     - cron: '0 0　20* * ?' # 20pm UTC - 5am JTC everyday
    workflow_dispatch:

jobs:
  curl:
    name: Kick deploy hook
    runs-on: ubuntu-latest
    steps:
      - name: curl
        run: curl -X POST '${{ secrets.DEPLOY_HOOK_URL }}'
